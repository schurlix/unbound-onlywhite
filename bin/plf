#!/usr/bin/env bash
cmd="$1"
ubctl="unbound-control -c /var/unbound/unbound.conf"

display() {
    $ubctl list_local_zones | egrep -v '^(OPNsense|localdomain)|(ip6|in-addr|home)\.arpa|(static|redirect)$'
}

if [ -z $cmd ]; then
    display
    exit
fi
