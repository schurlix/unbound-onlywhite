#!/usr/bin/env bash
cmd="$1"

display () {
    unbound-control list_local_zones | egrep -v '^(OPNsense|localdomain)|(ip6|in-addr|home)\.arpa|(static|redirect)$'
}

if [ -z $cmd ]
then
    display
    exit
fi

